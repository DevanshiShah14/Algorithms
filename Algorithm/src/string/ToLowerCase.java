package string;

/**
 * 709. Implement function ToLowerCase() that has a string parameter str, and
 * returns the same string in lowercase.
 *
 */
public class ToLowerCase {

	public String toLowerCase(String str) {
		str = str.toLowerCase();
		return str;
	}
}
